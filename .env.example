# 璇玑网关环境变量示例
# 复制为 .env 并填写真实值。
# config.yaml 中的 ${VAR} 占位符会从这里读取；已存在于进程环境中的变量不会被覆盖。

# 本机 vLLM 上游的 API 密钥
VLLM_KEY=sk-vllm-local-example

# OpenCode Go 上游的 API 密钥
OPENCODE_KEY=sk-opencode-go-example
